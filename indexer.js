var crawler = require('./crawler.js');
/*
function indexerFunc() {
	var first = queuedUrls.shift();
	if(first != null)
	{
		if(foundUrls.indexOf(first) == -1)
		{
			foundUrls.push(first);
			console.log("Indexed " + first);
		}
		else
		{
			console.log("Excluding duplicate url: " + first);
		}
	}
}

setInterval(indexerFunc, 200);*/